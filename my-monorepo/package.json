{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn install",
    "build": "yarn workspace api build && yarn workspace db generate",
    "start": "yarn workspace api start",
    "dev": "yarn workspace api dev"
  }
}
